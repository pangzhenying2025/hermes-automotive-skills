{
  "name": "vehicle-fleet-multi-cloud",
  "version": "1.0.0",
  "description": "Multi-cloud vehicle fleet infrastructure with Pulumi",
  "main": "index.ts",
  "scripts": {
    "build": "tsc",
    "deploy:aws": "pulumi config set cloud aws && pulumi up",
    "deploy:azure": "pulumi config set cloud azure && pulumi up",
    "deploy:gcp": "pulumi config set cloud gcp && pulumi up"
  },
  "dependencies": {
    "@pulumi/pulumi": "^3.0.0",
    "@pulumi/aws": "^6.0.0",
    "@pulumi/azure-native": "^2.0.0",
    "@pulumi/gcp": "^7.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
